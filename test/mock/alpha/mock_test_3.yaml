metadata:
  auth:
    basic:
      user: "mock_user"
      password: "mock_pass"
  url: "mock.site.localhost"
requests:
  test_get:
    - url: "/dummy/get"
      method: "get"
      json:
        name: "test_3"
      